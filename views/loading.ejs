<!-- loading.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Page Title</title>
    <!-- Add your CSS link here -->
    <style>
        /* CSS for the loading container */
        .loading-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background */
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999; /* Ensure it's on top of other elements */
        }

        /* CSS for the loading image */
        .loading-image {
            width: 100px; /* Adjust the size of the loading image as needed */
            height: 100px;
        }
    </style>
</head>
<body>
    <!-- Loading image container -->
    <div class="loading-container">
        <img src="asset/img/truck.gif.gif" alt="Loading" class="loading-image">
    </div>

    <!-- Your page content -->
    <div class="content">
        <!-- Your content here -->
    </div>

    <!-- Your JavaScript files -->
    <script>
        // JavaScript to hide the loading container when the page is fully loaded
        window.addEventListener('load', function() {
            var loadingContainer = document.querySelector('.loading-container');
            loadingContainer.style.display = 'none';
        });
    </script>
</body>
</html>
